line1 = ["⬜️","️⬜️","️⬜️"]
line2 = ["⬜️","⬜️","️⬜️"]
line3 = ["⬜️️","⬜️️","⬜️️"]
map = [line1, line2, line3]
print("Hiding your treasure! X marks the spot.")
position = input() # Where do you want to put the treasure?
# 🚨 Don't change the code above 👆
# Write your code below this row 👇
inner = position[0].lower()
abc = ["a", "b", "c"]
inner = abc.index(inner)
#print(position[0])
#inner = 
#if position[0] == "A":
#  inner = 0
#elif position[0] == "B":
#  inner = 1
#else:
#  inner = 2

outer = int(position[1])-1
print(f"outer {outer}")
#print(inner)
#print(outer)
map[outer][inner] = 'X'
#if outer == 0:
#  line1[inner] = 'X'
#elif outer == 1:
#  line2[inner] = 'X'
#else:
#  line3[inner] = 'X'

# Write your code above this row 👆
# 🚨 Don't change the code below 👇
print(f"{line1}\n{line2}\n{line3}")